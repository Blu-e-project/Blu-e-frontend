package com.example.blu_e.data.mypage

import android.os.Bundle
import androidx.fragment.app.Fragment
import com.example.blu_e.MainActivity
import com.example.blu_e.customercenter.CenterFragment
import com.example.blu_e.customercenter.QuestionDetailFragment
import com.example.blu_e.data.RetroInterface
import com.example.blu_e.data.customercenter.Question
import com.example.blu_e.databinding.FragmentQuestionDetailBinding
import com.example.blu_e.mypage.MentorMyReviewActivity

class ReviewDetailFragment: Fragment() {
    /*private lateinit var mContext: MainActivity
    private var reviewActivity: MentorMyReviewActivity= MentorMyReviewActivity()
    private lateinit var viewBinding: Fragmentre
    private lateinit var review : MentorReviewListData
    private val api = RetroInterface.create()

    companion object {
        fun newInstance(reviewList: ArrayList<MentorReviewListData>, reviewId: Int) = QuestionDetailFragment().apply {
            arguments = Bundle().apply {
                putSerializable("list", faqList)
                putInt("questionId", faqId)
            }
        }
    }*/

}